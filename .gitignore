node_modules/
dist/
*.log
.DS_Store
.env

# NOTE: public/voices/ is deliberately NOT ignored. The committed mp3s are what
# let a visitor hear a deck with no Azure key and no server running, so they're
# a shipped asset rather than a build artifact. Delete a file to force that
# slide to re-synthesize on the next bake.
